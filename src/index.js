// BWOC → OpenClaw plugin entry. Registers coordination tools that shell out to
// the `bwoc` CLI. WIP scaffold — see README.
export function register(api) {
  // TODO: api.on(...) / api.registerHook(...) wrapping `bwoc list/send/run/...`
  void api;
}
